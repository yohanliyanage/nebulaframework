Registration Queue
------------------

There will be one registration queue per running host. The cluster manager(s) will listen to this queue for registration requests. In case of running multiple CMs in single host, each CM will auto-load balance the incoming requests.

